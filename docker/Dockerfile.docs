# BookOne docs domain — reverse-proxy to web Fumadocs (identical UI/content)
# Not a second design system. Requires `web` service on Docker network.
FROM nginx:1.27-alpine
COPY docker/docs.nginx.conf /etc/nginx/conf.d/default.conf
EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
