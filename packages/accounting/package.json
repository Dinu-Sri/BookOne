{
  "name": "@bookone/accounting",
  "version": "2.0.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@bookone/db": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^2.1.0"
  }
}
